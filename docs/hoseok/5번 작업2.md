# 5번 남은 작업 체크리스트

이 문서는 5번 담당자의 다음 작업 후보를 PR 가능한 단위로 쪼갠 체크리스트입니다. 목표는 Auth/Admin/Infra/부하 테스트 범위를 명확히 하고, 다른 담당자의 feature/domain을 임의로 건드리지 않는 것입니다.

## 0. 작업 시작 전 확인

- [x] 이번 작업이 5번 담당 범위인지 확인한다.
- [x] 1번 Auth 화면 담당자와 겹치는지 확인한다.
- [x] 2번 부품/가격 담당자와 겹치는지 확인한다.
- [x] 3번 Agent/RAG/Tool 담당자와 겹치는지 확인한다.
- [x] 4번 PC Agent/AS 담당자와 겹치는지 확인한다.
- [x] API 요청/응답 구조 변경 여부를 확인한다.
- [x] API 계약이 바뀌면 `docs/openapi.yaml`도 같은 PR에서 수정한다.
- [x] page component에서 `api()`를 직접 호출하지 않고 담당 `*Api.ts`를 사용한다.
- [x] `components/ui.tsx`, `prototypeData.ts`, `AdminPages.tsx`는 barrel 전용으로 유지한다.

## 1. Auth 흐름 정리

### 1.1 현재 상태 확인

- [x] `apps/web/src/features/auth/AuthPages.tsx`의 로그인 submit 흐름을 확인한다.
- [x] 현재 로그인 값이 `user@example.com`, `password`로 하드코딩되어 있음을 확인한다.
- [x] `apps/web/src/features/auth/authApi.ts`의 `login`, `signup` wrapper를 확인한다.
- [x] `apps/web/src/lib/api.ts`의 token 저장 key가 `buildgraph.token`인지 확인한다.
- [x] 백엔드 `UserController`, `JwtTokenService`, `CurrentUserService`의 JWT 발급/검증 규칙을 확인한다.

### 1.2 1번 담당자와 경계 확인

- [x] 로그인/회원가입 form state 연결을 1번이 할지 5번이 할지 확인한다.
- [x] 5번은 인증 공통 정책과 관리자 권한 흐름만 담당한다는 경계를 확인한다.
- [x] 화면 UI 세부 개선은 1번 담당 범위인지 확인한다.
- [x] admin demo 계정 안내를 화면에 넣을지 문서에만 둘지 확인한다.

#### 결정 메모

- 로그인/회원가입 form state 연결은 1번 담당으로 확정한다.
- 5번은 Auth API, token 저장/전달, `RequireAdmin`, `auth/me` 정책까지만 담당한다.
- 로그인/회원가입 화면 UI 수정은 1번 담당으로 둔다.
- admin demo 계정 안내는 화면에 추가하지 않고 문서에만 둔다.
- `admin@example.com` 로그인으로 admin token을 받는 흐름은 5번이 문서화하고, 실제 로그인 화면 입력 연결은 1번에게 넘긴다.

### 1.3 admin demo 계정 안내

- [x] admin demo 계정 기준을 정한다.
- [x] 현재 seed 기준 `admin@example.com` 로그인 시 JWT access token이 발급되는지 확인한다.
- [x] 일반 user 계정과 admin 계정의 차이를 문서화한다.
- [x] 로그인 화면에 admin demo 안내를 넣는다면 문구를 짧게 작성한다.
- [x] 로그인 화면을 수정한다면 1번 담당자와 합의한 뒤 진행한다.

#### admin demo 계정 흐름

- 백엔드 DB seed 기준으로 `admin@example.com`은 관리자 계정으로 취급한다.
- `admin@example.com`으로 로그인하면 `role: "ADMIN"`과 JWT 형식의 `accessToken`을 반환한다.
- `user@example.com`으로 로그인하면 `role: "USER"`와 JWT 형식의 `accessToken`을 반환한다.
- 프론트 공통 API helper는 로그인 성공 token을 `buildgraph.token` key로 localStorage에 저장한다.
- `RequireAdmin`은 먼저 `buildgraph.token`이 있는지 확인한다.
- token이 없으면 `/api/auth/me`를 호출하지 않고 "관리자 권한이 필요합니다" 안내 화면을 보여준다.
- token이 있으면 `GET /api/auth/me`를 호출해 현재 사용자 role을 확인한다.
- `role: "ADMIN"`이면 `/admin/*` 화면 접근을 허용한다.
- `role: "USER"`이거나 `auth/me` 확인에 실패하면 관리자 화면 대신 "관리자 권한이 필요합니다" 안내 화면을 보여준다.

#### 담당 경계

- 5번은 위 JWT token 정책과 `RequireAdmin` guard 동작을 문서화한다.
- 로그인 화면에서 `admin@example.com`을 직접 입력해 로그인할 수 있게 form state를 연결하는 작업은 1번 담당이다.
- admin demo 계정 안내는 화면에 추가하지 않고 문서에만 둔다.

### 1.4 `auth/me` 호출 위치 결정

- [x] `GET /api/auth/me`를 앱 시작 시 호출할지 결정한다.
- [x] `RequireAdmin` 진입 시 호출할지 결정한다.
- [x] React Query로 사용자 세션을 관리할지 결정한다.
- [x] Sprint 1에서는 localStorage token 기반을 유지할지 결정한다.
- [x] `auth/me` 연결 시 실패 상태와 loading 상태를 정의한다.

#### 결정 메모

- 앱 시작 시점에는 `auth/me`를 호출하지 않는다.
- `/admin/*` 진입 시 `RequireAdmin`에서만 `auth/me`를 호출한다.
- token이 없으면 `auth/me`를 호출하지 않고 권한 안내 화면을 보여준다.
- token이 있으면 `auth/me`를 호출하고, 응답의 `role`이 `ADMIN`일 때만 관리자 화면을 보여준다.
- React Query 전역 세션 관리는 이번 단계에서는 도입하지 않고 `RequireAdmin` 내부 state로 처리한다.
- `auth/me` loading 중에는 "관리자 권한 확인 중" 화면을 보여준다.
- `auth/me` 실패 또는 `role !== "ADMIN"`이면 권한 안내 화면을 보여준다.

## 2. 관리자 권한 체크 고도화

### 2.1 현재 skeleton 기준 확인

- [x] `RequireAdmin`이 localStorage token 존재 여부를 먼저 확인하는지 확인한다.
- [x] token이 있으면 `GET /api/auth/me`로 role을 확인하는지 확인한다.
- [x] `role: "ADMIN"`이면 `/admin/*`에 접근 가능한지 확인한다.
- [x] token이 없거나 user role이면 안내 화면이 보이는지 확인한다.
- [x] 현재 구현이 Sprint 1 skeleton이라는 점을 문서화한다.

### 2.2 role 기반 권한 판단 정책

- [x] 백엔드 `AuthResponse.user.role` 값을 기준으로 권한 판단할지 결정한다.
- [x] `GET /api/auth/me` 응답의 `role`을 기준으로 권한 판단할지 결정한다.
- [x] localStorage token 문자열 검사 방식을 언제 제거할지 정한다.
- [x] 관리자 권한 확인 실패 시 화면 표시 방식과 redirect 방식을 비교한다.
- [x] Sprint 1에서는 안내 화면 유지 여부를 결정한다.

#### 결정 메모

- 관리자 route guard는 `AuthResponse.user.role` 대신 `GET /api/auth/me` 응답의 `role`을 기준으로 판단한다.
- localStorage token 문자열 내용을 직접 검사하는 방식은 제거한다.
- localStorage는 로그인 token 존재 여부를 빠르게 확인하는 용도로만 사용한다.
- 권한 확인 실패 시 redirect하지 않고 "관리자 권한이 필요합니다" 안내 화면을 유지한다.

### 2.3 401/403 정책 확정

- [x] 로그인하지 않은 사용자는 401로 볼지 정한다.
- [x] 로그인했지만 관리자 권한이 없으면 403으로 볼지 정한다.
- [x] 프론트에서 401과 403을 다른 메시지로 보여줄지 정한다.
- [x] API error response 공통 schema가 필요한지 확인한다.
- [x] 정책이 확정되면 `docs/openapi.yaml` 반영 필요 여부를 확인한다.

#### 결정 메모

- 로그인하지 않은 사용자는 `401 UNAUTHORIZED`로 본다.
- 로그인했지만 관리자 권한이 없는 사용자는 `403 FORBIDDEN`으로 본다.
- 프론트는 401일 때 로그인 필요 메시지, 403일 때 관리자 권한 없음 메시지를 보여준다.
- API error response는 `{ code, message }` 공통 schema를 사용한다.
- 이 정책은 `docs/openapi.yaml`의 `ErrorResponse`, `UnauthorizedError`, `ForbiddenError`, `/api/auth/me`, `/api/admin/*` 응답 계약에 반영한다.

### 2.4 OpenAPI 반영 여부 확인

- [x] `/api/admin/*` 경로에 bearerAuth 표시가 필요한지 확인한다.
- [x] `/api/auth/me` 응답 schema 상세화가 필요한지 확인한다.
- [x] 401/403 response를 OpenAPI에 추가할지 결정한다.
- [x] 공통 error schema를 추가할지 결정한다.
- [x] 결정 전에는 임의로 OpenAPI를 확장하지 않는다.

#### 결정 메모

- `/api/admin/*` 경로에는 `bearerAuth` 보안 요구사항을 표시한다.
- `/api/auth/me`의 200 응답은 `User`, 401 응답은 `ErrorResponse`로 명시한다.
- `/api/admin/*`의 401/403 응답은 공통 response component로 명시한다.
- 정책이 사용자에 의해 확정되었으므로 OpenAPI 확장을 진행한다.
- OpenAPI에는 `ErrorResponse`, `UnauthorizedError`, `ForbiddenError`, `/api/auth/me`, `/api/admin/*` 보안 요구사항과 401/403 응답을 반영했다.

## 3. Admin API 연결 정리

### 3.1 연결 우선순위 확정

- [x] 5번 소유인 `AdminDashboardPage.tsx`부터 연결한다.
- [x] `apps/web/src/features/admin/adminApi.ts`의 `getAdminDashboard()`를 사용한다.
- [x] page에서 `api()`를 직접 호출하지 않는다.
- [x] 3번/4번 소유 관리자 page는 임의로 연결하지 않는다.
- [ ] Agent/RAG/Tool/AS 화면 연결은 각 담당자와 합의 후 진행한다.

#### 결정 메모

- 이번 단계에서는 5번 소유인 `AdminDashboardPage.tsx`만 `getAdminDashboard()`로 연결한다.
- `AgentSessionAdminPage.tsx`, `ToolInvocationAdminPage.tsx`, `RagEvidenceAdminPage.tsx`, `AdminTicketsPage.tsx`, `AdminTicketDetailPage.tsx`, `AdminPartsPage.tsx`는 각 담당자 합의 전까지 API 연결을 진행하지 않는다.

### 3.2 AdminDashboardPage API 연결

- [x] 현재 dashboard mock 데이터 위치를 확인한다.
- [x] `getAdminDashboard()` 응답 필드와 화면 표시 필드를 비교한다.
- [x] loading 상태를 추가한다.
- [x] error 상태를 추가한다.
- [ ] success 상태에서 API 응답을 표시한다.
- [x] API 서버가 꺼져 있을 때 route smoke test가 깨지지 않는지 확인한다.
- [x] 필요하면 seed fallback 사용 여부를 결정한다.

#### 결정 메모

- `AdminDashboardPage.tsx`는 현재 `getAdminDashboard()`를 호출한다.
- 프론트 `adminApi.ts`/화면은 `llmQueueP95`, `apiP95`, `asOpen`, `recommendationSuccess`를 기대한다.
- 백엔드 `AdminSeed.dashboard()`와 OpenAPI `AdminDashboardDto`는 `agentRunning`, `openTickets`, `priceJobsRunning`, `degraded`, `generatedAt`를 반환하도록 바뀌어 있다.
- 따라서 AdminDashboard API 연결 자체는 되어 있지만, success 상태의 실제 표시 필드 정합성은 아직 미완료다.
- API 조회 중에는 loading 안내를 보여준다.
- API 조회 실패 시 seed fallback을 쓰지 않고 error 안내를 보여준다.
- 기존 최근 Agent 세션/운영 경고 표시는 아직 dashboard page 내부 seed 표시로 남긴다.
- API 서버가 꺼진 route smoke는 token 없는 `/admin/*` 접근에서 guard 화면으로 종료되므로 관리자 API를 호출하지 않고 통과할 수 있다.

### 3.3 adminApi 타입 정리

- [x] `getAdminDashboard()` 반환 타입을 정의할지 확인한다.
- [x] dashboard metric 타입을 정의한다.
- [ ] API 응답 필드명과 화면 필드명을 맞춘다.
- [x] API response schema를 OpenAPI에 상세화할 필요가 있는지 확인한다.
- [x] 타입 추가는 `adminApi.ts` 또는 admin feature 내부에 둔다.

#### 결정 메모

- `AdminDashboard` 타입은 `apps/web/src/features/admin/adminApi.ts`에 둔다.
- `AdminDashboardPage.tsx`는 `getAdminDashboard()`만 호출하고 `api()`를 직접 호출하지 않는다.
- OpenAPI에는 admin dashboard 200 응답 schema인 `AdminDashboardDto`가 상세화되어 있다.
- 후속 작업은 프론트 `AdminDashboard` 타입과 화면 필드를 `AdminDashboardDto`에 맞추는 것이다.

### 3.4 다른 admin page 연결 경계

- [ ] `AgentSessionAdminPage.tsx` 연결 전 3번 담당자와 합의한다.
- [ ] `ToolInvocationAdminPage.tsx` 연결 전 3번 담당자와 합의한다.
- [ ] `RagEvidenceAdminPage.tsx` 연결 전 3번 담당자와 합의한다.
- [ ] `AdminTicketsPage.tsx` 연결 전 4번 담당자와 합의한다.
- [ ] `AdminTicketDetailPage.tsx` 연결 전 4번 담당자와 합의한다.
- [ ] `AdminPartsPage.tsx` 연결 전 2번 담당자와 합의한다.

## 4. 인프라 실제 동작 확인

### 4.1 Docker Compose 기준 확인

- [x] `compose.yaml`의 postgres 서비스를 확인한다.
- [x] `compose.yaml`의 redis 서비스를 확인한다.
- [x] `compose.yaml`의 rabbitmq 서비스를 확인한다.
- [x] `compose.yaml`의 mailpit 서비스를 확인한다.
- [x] `compose.yaml`의 api/web 의존 관계를 확인한다.
- [x] `docker compose config`를 실행한다.
- [ ] `docker compose up --build`로 전체 실행을 확인한다.

#### 확인 근거

- `compose.yaml`에 postgres, redis, rabbitmq, mailpit, api, web 서비스가 정의되어 있다.
- `api`는 postgres healthcheck, redis, rabbitmq에 의존한다.
- `web`은 `api`에 의존하고 `VITE_API_BASE_URL=http://localhost:8080`을 사용한다.
- `docker compose config` 실행 결과가 정상 출력되었다.

### 4.2 Redis 검증 범위 결정

- [ ] Redis를 어떤 기능에서 사용할지 정한다.
- [ ] 추천 결과 cache인지, 세션 cache인지, job 상태 cache인지 결정한다.
- [ ] 담당 feature가 5번 범위를 넘는지 확인한다.
- [ ] Sprint 1에서는 연결 확인만 할지 실제 코드 사용까지 할지 정한다.
- [ ] Redis 검증 명령 또는 health check 방식을 문서화한다.

### 4.3 RabbitMQ 검증 범위 결정

- [ ] RabbitMQ를 어떤 Job에서 사용할지 정한다.
- [ ] 가격 수집 Job인지, Agent Job인지, 메일 Job인지 확인한다.
- [ ] 2번/3번 담당자와 겹치는지 확인한다.
- [ ] Sprint 1에서는 queue 생성 확인만 할지 실제 publish/consume까지 할지 정한다.
- [ ] RabbitMQ 관리 화면 접속 방법을 문서화한다.

### 4.4 Mailpit 검증 범위 결정

- [ ] 어떤 메일을 발송 테스트할지 정한다.
- [ ] 회원가입 인증 메일인지, 목표가 알림 메일인지, 관리자 알림 메일인지 확인한다.
- [ ] 목표가 알림은 2번 담당 영역이므로 경계를 확인한다.
- [ ] Sprint 1에서는 Mailpit 실행 확인만 할지 실제 메일 발송까지 할지 정한다.
- [ ] Mailpit 접속 주소와 확인 방법을 문서화한다.

## 5. k6 / 성능 리포트

### 5.1 현재 k6 skeleton 확인

- [x] `infra/k6/smoke.js`를 확인한다.
- [x] 현재 `/api/health` 요청을 확인한다.
- [x] 현재 `/api/builds/recommend` 요청을 확인한다.
- [x] 현재 `/api/parts` 요청을 확인한다.
- [x] smoke 목적과 부하 테스트 목적을 구분한다.

#### 확인 근거

- 현재 k6는 `constant-vus`, `vus: 5`, `duration: 30s`로 `/api/health`, `/api/builds/recommend`, `/api/parts`를 확인하는 smoke 수준이다.
- threshold는 `http_req_failed < 0.01`, `p95 < 500ms`다.
- 300명/1000명 부하 테스트 완성본은 아직 별도 시나리오로 확정되지 않았다.

### 5.2 부하 테스트 시나리오 확정

- [x] Sprint 1에서 실행할 smoke 수준 시나리오를 정한다. 결정: `infra/k6/smoke.js` 기준 5 VU, 30초 smoke로 둔다.
- [x] Sprint 후반에 확장할 부하 테스트 시나리오를 정한다. 결정: 300명/1,000명 부하는 별도 시나리오와 별도 script로 확장한다.
- [x] 300명/1000명 목표가 이번 Sprint인지 이후 Sprint인지 확인한다. 결정: 300명은 2주차, 1,000명은 4주차 목표로 둔다.
- [ ] 사용자 견적 생성 흐름을 포함할지 정한다.
- [ ] 관리자 조회 흐름을 포함할지 정한다.
- [ ] AS 티켓 생성 흐름을 포함할지 정한다.

### 5.3 성능 리포트 템플릿 작성

- [x] 리포트 파일 위치를 정한다. 위치: `docs/reports/k6-smoke-report-template.md`
- [x] 테스트 일시를 기록하는 항목을 둔다.
- [x] 실행 환경을 기록하는 항목을 둔다.
- [x] 시나리오와 VU 수를 기록하는 항목을 둔다.
- [x] p95 latency를 기록하는 항목을 둔다.
- [x] error rate를 기록하는 항목을 둔다.
- [x] 병목 후보와 후속 조치를 기록하는 항목을 둔다.

## 6. PR 전 검증

- [x] `cd apps/web`를 실행한다.
- [ ] `npm run build`를 실행한다.
- [x] `npm run test`를 실행한다.
- [x] repository root로 돌아간다.
- [x] `python tools/validate_openapi.py`를 실행한다.
- [x] `docker compose config`를 실행한다.
- [ ] 백엔드 변경이 있으면 `cd apps/api`를 실행한다.
- [ ] 백엔드 변경이 있으면 `./gradlew bootJar --no-daemon`을 실행한다.
- [ ] Docker/인프라 변경이 있으면 `docker compose up --build`를 확인한다.

#### 검증 근거

- `apps/web/test-results/.last-run.json` 기준 Playwright 상태는 `passed`다.
- `PYTHONPATH=.venv/lib/python3.11/site-packages python3 tools/validate_openapi.py` 실행 결과 `OpenAPI validation passed: 42 paths`를 확인했다.
- 기본 `python3 tools/validate_openapi.py`는 현재 셸에서 PyYAML 경로를 찾지 못해 실패하므로, 로컬 검증은 venv 활성화 또는 위 PYTHONPATH 방식이 필요하다.
- `docker compose config` 실행 결과 compose 설정이 정상 출력되었다.

## 7. 완료 기준

- [x] Auth 흐름에서 1번과 5번 책임 경계가 명확하다.
- [x] 관리자 권한 체크가 현재 Sprint 기준으로 설명 가능하다.
- [x] AdminDashboard API 연결 여부와 다음 담당 경계가 명확하다.
- [ ] Redis/RabbitMQ/Mailpit 검증 범위가 정해졌다.
- [ ] k6 smoke와 부하 테스트 확장 방향이 구분되어 있다.
- [x] OpenAPI 수정 필요 여부가 판단되어 있다.
- [x] 다른 담당자 feature/domain을 임의로 수정하지 않았다.
- [ ] PR 검증 명령이 통과했다.

#### 남은 미완료 근거

- AdminDashboard는 프론트 기대 필드와 OpenAPI/API seed 필드가 달라 success 상태 실제 표시 정합성이 아직 맞지 않는다.
- `npm run build`, `./gradlew bootJar --no-daemon`, `docker compose up --build`는 이번 확인에서 실행하지 않았다.
- Redis/RabbitMQ/Mailpit은 compose 서비스 확인은 끝났지만, Sprint 1 검증 범위와 실제 사용 여부가 아직 정해지지 않았다.
- k6는 현재 smoke skeleton 확인만 끝났고, 부하 테스트 확장 시나리오는 아직 확정되지 않았다.
